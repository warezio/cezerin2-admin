import React,{useEffect} from 'react'
import * as auth from '../lib/auth'

const Logout = () => {
	useEffect(()=>
	useEffect(,[]Mount() {
		auth.removeToken()
	})

		return null
}

export default Logout