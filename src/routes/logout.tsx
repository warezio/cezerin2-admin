import React,{useEffect} from 'react'
import * as auth from '../lib/auth'

const Logout = () => {
	useEffect(()=>
	componentWillMount() {
		auth.removeToken()
	})

		return null
}

export default Logout